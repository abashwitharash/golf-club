<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/show.css" />
    <title>Golf Game: <%= golf.course %></title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>

    <div class="golf-container">
      <% if (golf.course) { %>
        <h1 class="golf-title">Golf Course: <%= golf.course %></h1>
      <% } %>

      <% if (golf.holes) { %>
        <p class="golf-info">Holes Played: <%= golf.holes %></p>
      <% } %>

      <% if (golf.score) { %>
        <p class="golf-info">Score: <%= golf.score %></p>
      <% } %>

      <% if (golf.difficulty && golf.difficulty !== 'none') { %>
        <p class="golf-info">Difficulty: <%= golf.difficulty %></p>
      <% } %>

      <% if (golf.notes && golf.notes !== 'none') { %>
        <p class="golf-notes">Notes: <%= golf.notes %></p>
      <% } %>
      
      <div class="golf-actions">
        <a href="/users/<%= user._id %>/golfs/<%= golf._id %>/edit" class="golf-edit">Edit this Game</a>

        <form action="/users/<%= user._id %>/golfs/<%= golf._id %>?_method=DELETE" method="POST">
          <button type="submit" class="golf-delete">Delete this Game</button>
        </form>
      </div>
    </div>
  </body>
</html>
