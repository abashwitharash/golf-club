<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Community Page</title>
  </head>
  <body>

    <%- include('../partials/_navbar.ejs') %>

    <div class="all-golfs-body">
      <div class="content-container">
        
        <div class="left-section">
          <div class="header-row">
            <h1 class="all-golfs-header">Explore Our Community</h1>
          </div>
          <ul class="game-list">
            <% users.forEach(communityUser => { %>
              <li>
                <a href="/users/<%= user._id %>/users/<%= communityUser._id %>">
                  <%= communityUser.username %>
                </a>
              </li>
            <% }) %>
          </ul>
        </div>
        <div class="right-section">
          <img src="https://i.imgur.com/nOI00h8.jpeg" 
               alt="Golfers celebrating" 
               class="golf-image">
        </div>
      </div>
    </div>

  </body>
</html>